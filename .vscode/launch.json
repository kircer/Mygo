{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "调试 Vue 应用",
            "url": "http://localhost:5173", // 确保这个地址和终端输出的一致
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}
